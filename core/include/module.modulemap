// Exposes the generated C header to Swift as `import CMarkDev`.
//
// The header itself is generated by cbindgen during `cargo build`, so this
// modulemap is the only hand-written file in this directory.
module CMarkDev {
    header "markdev.h"
    export *
}
